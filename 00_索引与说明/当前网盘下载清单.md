# 当前网盘下载清单

> 文档同步日期：2026-08-03
> 最新实际学习记录：2026-08-03
> 官方网盘目录索引最后刷新：2026-07-23 16:07:39 +08:00
> 当前停点：第 8 课显示链路只读盘点与问答验收已完成；第 9 课官方 MIPI 镜像硬件基线尚未开始。

## 1. 当前结论

**当前没有必须继续下载的新资料。**

第 8 课已经使用现有 Buildroot 完成显示链路只读盘点。进入第 9 课仍不需要下载新资料；官方 MIPI 镜像已经准备好，但必须先完成课前诊断、接线核对和恢复方案确认。

已在 2026-07-26 下载并完成完整性核对的两项大型资料，已经覆盖显示验证和后续 SDK 学习的主线需要：

1. 官方 kernel 6.1 Linux SDK 全部 12 个分卷及配套说明文件；
2. Debian 12 kernel 6.1 的 3.1 寸 MIPI 镜像。

后续使用原则：

```text
第 8 课：只读盘点当前 Buildroot 显示链路（已完成）
  → 未解压 SDK、未烧录镜像
第 9 课：建立屏幕工作基线
  → 在确认串口恢复路径、接线和烧录流程后，使用已下载的官方 MIPI Debian 镜像
第 10 课及以后：SDK / DTB / 内核编译
  → 在 Ubuntu 的 ext4 文件系统中解压和使用 SDK
```

## 2. 已下载的大型资料

### A. 官方 kernel 6.1 Linux SDK

本地目录：

```text
网盘资料\第05章.【立创·泰山派】系统SDK\【SDK】Linux-kernel6.1\tSpi-1M-lInux-kernel6.1-sdk-20260403\
```

当前状态：

```text
分卷：part.00～part.11，共 12 个
分卷总大小：25,563,903,512 bytes（23.808 GiB）
配套文件：README.md、QUICK_START.txt、extract_sdk.sh、file_list.txt、file_checksums.sha256 等
```

完整性检查：

- 12 个分卷数量正确；
- 每个分卷大小均与官方 README 和网盘索引一致；
- 2026-07-26 22:37:26 +08:00 已对 12 个分卷执行 SHA-256 复核；
- 全部分卷与 `sdk_parts_checksums.sha256` 一致。

使用边界：

- 当前保持归档，不在 Windows NTFS 工作目录直接解压；
- 第 8、9 课不需要解压；
- 第 10 课开始前，应将 SDK 放到 Ubuntu / ext4 文件系统，并预留约 96～144 GiB 的合并、解压和构建空间；
- 后续只使用与当前目标系统、内核版本匹配的 SDK 和工具链进行 DTB、内核或模块构建。

### B. Debian 12 3.1 寸 MIPI 镜像

本地路径：

```text
网盘资料\第06章.【立创·泰山派】系统镜像\kernel6.1\【镜像】Debian12\debian_kernel6.1_img\tspi-1m-kernel6.1-debian-3.1inch-mipi-20260331.img
```

当前状态：

```text
大小：4,787,677,770 bytes（4.459 GiB）
大小与网盘索引一致
尚未烧录
```

使用边界：

- 第 8 课只读盘点已完成，期间未烧录；
- 第 9 课用于建立官方 MIPI 屏、背光、触摸和网络的工作基线；
- 烧录前必须记录当前 Buildroot 镜像信息、串口参数和恢复流程；
- 串口始终作为烧录后首选观察与恢复通道。

## 3. 当前已有的轻量资料

| 资料 | 状态 |
|---|---|
| 开发板原理图 | 已有 |
| 官方硬件手册 | 已有 |
| IO 口分配表 | 已有 |
| Buildroot 系统使用文档 | 已有 |
| Linux SDK 编译说明 | 已有 |
| kernel 6.1 Buildroot HDMI 镜像 | 已有且已烧录验证 |
| GPIO / libgpiod 离线 Wiki | 已有 |
| C / C++ 交叉编译离线 Wiki | 已有 |
| D310T9362V1 规格书、补丁、DTSI、boot 镜像 | 已有 |
| 公共 Linux 5.10 学习 SDK | 已有，仅用于结构学习 |

## 4. 后续仍可能按需下载

以下资料当前仍不是主线必需，不进行整库下载：

- 3.1 寸屏 EDA 工程；
- 3.1 寸屏 6P 与 31P 排线工程；
- Android 11 / 13 SDK 和其他 Android 镜像；
- Linux kernel 4.9 SDK；
- 其他 Debian、Ubuntu、Buildroot 镜像；
- 第 09 章全部项目案例；
- 与 D310T9362V1 无关的其他屏幕资料。

只有在进入画板、改板、Android 或特定项目时再单独获取，避免下载约 279.77 GB 的完整网盘资料。

## 5. GitHub 同步边界

大型 SDK、镜像、离线 Wiki、硬件资料和下载目录均由 `.gitignore` 排除，只保留在本机，不上传 GitHub。

GitHub 仅同步：

- 学习课程和记录；
- 进度、路线、计划和下载状态说明；
- 轻量脚本；
- 小型日志、截图和工具文档。